version: '2.1'
services:
  rabbitmq:
    container_name: rabbitmq-fwmtadapter-it
    image: rabbitmq:3.6.10-management
    ports:
      - "34369:4369"
      - "55672:25672"
      - "35671:5671"
      - "35672:5672"
      - "46671:15671"
      - "46672:15672"

#  postgres-database:
#    container_name: postgres-case-api-it
#    image: sdcplatform/ras-rm-docker-postgres
#    command: ["-c", "shared_buffers=256MB", "-c", "max_connections=200"]
#    ports:
#      - "16432:5432"
#    healthcheck:
#      test: ["CMD-SHELL", "pg_isready -U postgres"]
#      interval: 10s
#      timeout: 5s
#      retries: 5
#
#  case-api:
#    container_name: case-api-it
#    image: eu.gcr.io/census-rm-ci/rm/census-rm-case-api
#    environment:
#      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres-case-api-it:5432/postgres?sslmode=disable
#      - JAVA_OPTS=-Xmx128m -Xdebug -Xrunjdwp:transport=dt_socket,server=y,suspend=n,address=5121 -Dspring.profiles.active=dev
#    healthcheck:
#      test: ["CMD", "cat", "/tmp/case-api-healthy"]
#      interval: 30s
#      timeout: 10s
#      retries: 10
#    depends_on:
#      postgres-database:
#        condition: service_healthy

